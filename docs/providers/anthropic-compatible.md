# Anthropic-compatible providers

The shared Messages adapter supports streaming content blocks, tool use, usage,
timeouts and retries. A configurable Base URL is accepted, while authentication
remains in the OS credential store. Provider-specific capabilities remain
unknown unless documented, contract-tested, or explicitly overridden.

